[package]
name = "nenemu_sdl2"
version = "0.1.0"
edition = "2024"
# build = "build.rs"
default-run = "audio_sync"

[features]
savestates = ["nenemu_core/savestates"]
static = ["sdl2/static-link"]

[dependencies]
sdl2 = "0.38.0"
nenemu_core = { path = "../nenemu_core" }
